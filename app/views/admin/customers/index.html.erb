<h5 class="bg-secondary" style="width: 90px; margin:30px 0 20px 200px;">会員一覧</h5>

<div class="container">
  <div class="col-1"></div>
  <div class="col-10">
    <table class="table table">
      <thead class="table-active">
        <tr>
          <th><b>会員ID</b></th>
          <th><b class="name">企業名</b></th>
          <th>メールアドレス</th>
          <th>ステータス</th>
        </tr>
      </thead>
      <% @customers.each do |customers| %>
      <tbody>
        <tr>
          <td><%= customers.id %></td>
          <td>
            <%= link_to admin_customer_path(customers.id,), class:"name" do %>
               <%= customers.enterprise_name %>
            <% end %>
          </td>
          <td><%= customers.email %></td>
          <td>
            <% if customers.is_customer_status? %>
              <p class="font-weight-bolder text-success">
                <%= '有効' %>
              </p>
            <% else %>
              <p class="font-weight-bolder text-muted">
                <%= '退会' %>
              </p>
            <% end %>
          </td>
        </tr>
      </tbody>
      <% end %>
    </table>
  </div>
  <%= paginate @customers, per_page: 10 %>
  <div class="col-1"></div>
</div>
