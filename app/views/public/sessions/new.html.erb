<div class="container">
  <div class="row">
    <div class="mx-auto">

      <h2>ログイン</h2>
      <p>会員の方はこちらからログイン</p>
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

      <table>
        <tr>
          <td class="field">
            <%= f.label :メールアドレス, class: "mr-3" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
          </td>
        </tr>

        <tr>
          <td class="field">
            <%= f.label :パスワード, class: "mr-5" %>
            <%= f.password_field :password, autocomplete: "current-password" %>
          </td>
        </tr>
        
        <tr>
          <td class="actions">
            <%= f.submit "ログイン", class: "btn btn-outline-success rounded-pill mb-2" %>
          </td>
        </tr>
      </table>
      <% end %>

      <p>会員登録がお済でない方</p>
      <p><%= render "public/shared/links" %>から会員登録してください。</p>
      <p><%= link_to "ゲストログイン（閲覧用）", customers_guest_sign_in_path, class: "btn btn-secondary btn-sm btn-block mb-3 sign_in", method: :post %></p>
    
    </div>
  </div>
</div>